# Drop zone - local staging area for files, not committed
drop-zone/*
!drop-zone/.gitkeep

# SSL certs - generated locally
infrastructure/ssl/*.pem
infrastructure/ssl/*.key
infrastructure/ssl/*.crt

# Secrets
.env
*.env.local
credentials.json

# OS
.DS_Store
Thumbs.db

# Temp files
*.tmp
*.bak
/tmp/

# Go binaries
clood
clood-cli/clood
clood-cli/.clood/

# Session handoffs - INTENTIONALLY TRACKED for cross-machine context
# LAST_SESSION.md is NOT ignored - it passes context between agents
repomix-output.xml
